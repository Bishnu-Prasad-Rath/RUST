pub enum Status {
    Connected,
    Interupted,
}

pub fn connect_to_database() -> Status {
    //Connect do DB
    Status::Connected
}

pub fn get_user() {
    //Fetch the user from dn and return
}
