pub fn my_fn() {}

//This is not in the scope of the module tree means we can't compile this code but we can compile it if we write a line in lib.rs so lib.rs will consider the part of the module tree and compile as well.
